<Page x:Class="ProjecteBotigaSabates.Views.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjecteBotigaSabates.Views" xmlns:components="clr-namespace:ProjecteBotigaSabates.Components"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsPage"
      Loaded="Page_Loaded">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" >
            <Label Content="Search: " />
            <TextBox x:Name="tb_search" VerticalContentAlignment="Center" Width="200" Margin="0 0 20 0" KeyDown="Button_Search_Click" HorizontalAlignment="Center"></TextBox>
            <Button Content="Search" Width="80" Click="Button_Search_Click" HorizontalAlignment="Center"></Button>
        </StackPanel>


        <Grid>
            <!--Filtres de rang, color, preu ... -->
            <TextBlock Text="Rango"></TextBlock>
            
        </Grid>

        <ListView x:Name="lv_products" ItemsSource="{Binding Products}" Grid.Column="2" Grid.Row="2" Margin="10" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto" SelectionMode="Single" >
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Gray"/>
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Focusable" Value="False"/>

                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>

                    
                </Style>
            </ListView.ItemContainerStyle>


            <ListView.ItemTemplate>
                <DataTemplate>
                    <components:UCProduct Height="300" Margin="10" MyProd="{Binding}" ></components:UCProduct>
                </DataTemplate>
            </ListView.ItemTemplate>
            
            
        </ListView>
        
    </Grid>
</Page>
