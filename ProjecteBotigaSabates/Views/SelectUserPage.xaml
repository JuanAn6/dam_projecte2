<Page x:Class="ProjecteBotigaSabates.Views.SelectUserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjecteBotigaSabates.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SelectUserPage"
      Loaded="Window_Loaded">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <!--<Button Click="Button_Click_Insert_Data" Content="Insert test data" Grid.Column="0" Grid.Row="0"></Button>-->

        <ComboBox x:Name="cb_clients" Grid.Column="1" Grid.Row="1" MinWidth="200" Margin="10"></ComboBox>
        <Button  Content="Start!" Grid.Column="1" Grid.Row="2" Width="100" Click="Button_Click"></Button>

        <TextBlock Grid.ColumnSpan="100" x:Name="tb_info" Grid.Row="100" TextWrapping="Wrap"></TextBlock>
    </Grid>
</Page>
